This method takes two linked lists, where each node has a single digit integer value, and sums the value for the two coresponding nodes. The resulting sum is stored in
a result node that the method returns. If one list has more nodes than the other, it treats the "missing" nodes as zeros. If the sum is larger than 9 it adds the
carry over to the next node value. 
